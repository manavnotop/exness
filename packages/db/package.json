{
  "name": "@repo/db",
  "version": "1.0.0",
  "main": "index.js",
  "exports": {
    "./client": "./src/index.ts"
  },
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "setup:db": "docker exec -i postgres_db psql -U postgres -d postgres < ./setup_timescaledb.sql",
    "verify:db": "docker exec -i postgres_db psql -U postgres -d postgres < ./verify_timescaledb.sql"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "6.2.1",
    "@types/pg": "^8.15.5",
    "pg": "^8.16.3",
    "prisma": "^6.2.1"
  }
}